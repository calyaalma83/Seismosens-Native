{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Open seismosens.html",
            "file": "${workspaceFolder}/seismosens.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}